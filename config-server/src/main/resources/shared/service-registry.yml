eureka:
  client:
    # Disable the client side behaviour in standalone mode
    registerWithEureka: false
    fetchRegistry: false
    serviceUrl:
      defaultZone: http://service-registry:8761/eureka/
